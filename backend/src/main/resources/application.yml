server:
  port: 8081
  servlet:
    context-path: /api

# JWT配置（生产环境请通过环境变量/配置中心覆盖 secret）
icps:
  jwt:
    secret: ${ICPS_JWT_SECRET:icps-default-secret-please-change-in-production-0123456789}
    # 令牌有效期，单位毫秒，默认 2 小时
    expiration: ${ICPS_JWT_EXPIRATION:7200000}

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/icpsdb?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root
    

        
  # MyBatisPlus配置
  mybatis-plus:
    configuration:
      map-underscore-to-camel-case: true
      log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    global-config:
      db-config:
        id-type: auto
        logic-delete-field: deleted
        logic-delete-value: 1
        logic-not-delete-value: 0
        
  # Flyway数据库迁移配置
  flyway:
    enabled: false
    locations: classpath:db/migration
    baseline-on-migrate: true
    validate-on-migrate: true
        
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai
    
logging:
  level:
    com.icps: DEBUG
    org.springframework.jdbc.core: DEBUG
  
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{50} - %msg%n"
    
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always